#include "main.h"
/**
 * main - Entry point of code
 *
 * @argc: the number of times the command is passed
 * @argv: Array of the code passed.
 *
 * Return: return 0 (Success).
 */
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
