#include "main.h"
/**
 * mul - this is the main function that multiply
 *
 * @a: first declare integer.
 * @b: second declared integer
 *
 * Return: returns the product of a and b.
 */

int mul(int a, int b)
{
	return (a * b);
}
